---
phase: 02-backend-diarization
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/backend/realtime-service/src/services/presencialSessionManager.ts
autonomous: true
requirements: [DIAR-01, DIAR-02, DIAR-03, DIAR-05]

must_haves:
  truths:
    - "5s audio chunks are buffered in memory and flushed as 60s batches to Deepgram"
    - "Deepgram pre-recorded API is called with utterances:true and diarize:true for batch audio"
    - "Speaker and speaker_confidence are extracted from each utterance in Deepgram response"
    - "Utterances with diarization_confidence below 0.7 are marked needs_review=true with speaker='unknown'"
    - "Immediate unattributed transcriptions continue to be emitted for responsiveness"
    - "Diarized batch results emit presencialDiarizedBatch event to the room"
    - "Session end flushes remaining buffered audio and clears timer"
  artifacts:
    - path: "apps/backend/realtime-service/src/services/presencialSessionManager.ts"
      provides: "Batch accumulator, diarized processing, dual-path (immediate + batch)"
      contains: "flushBatch"
      min_lines: 200
  key_links:
    - from: "apps/backend/realtime-service/src/services/presencialSessionManager.ts"
      to: "apps/backend/realtime-service/src/config/database.ts"
      via: "db.saveDiarizedUtterances() call in processDiarizedUtterances"
      pattern: "saveDiarizedUtterances"
    - from: "apps/backend/realtime-service/src/services/presencialSessionManager.ts"
      to: "apps/backend/realtime-service/src/types/diarization.ts"
      via: "Import of AudioAccumulator, DiarizedUtterance, BatchResult types"
      pattern: "import.*AudioAccumulator"
---

<objective>
Implement the audio batch accumulator and diarized transcription processing in presencialSessionManager.ts.

Purpose: This is the core diarization pipeline — accumulates 5s client chunks into 60s batches, sends to Deepgram with utterances+diarize, extracts speaker attribution, applies confidence threshold, and stores per-utterance results. Also emits Socket.IO events for both immediate (unattributed) and batch (diarized) transcriptions.
Output: Refactored presencialSessionManager.ts with dual-path audio processing (immediate + batch accumulation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-diarization/02-CONTEXT.md
@.planning/phases/02-backend-diarization/02-RESEARCH.md
@.planning/phases/02-backend-diarization/02-01-SUMMARY.md

@apps/backend/realtime-service/src/services/presencialSessionManager.ts
@apps/backend/realtime-service/src/config/database.ts

<interfaces>
<!-- From Plan 01: types/diarization.ts -->
export interface AudioAccumulator {
  chunks: Buffer[];
  timer: NodeJS.Timeout | null;
  batchNumber: number;
  startTime: Date;
  totalDurationMs: number;
}

export interface DiarizedUtterance {
  speakerId: string;
  transcript: string;
  startMs: number;
  endMs: number;
  transcriptionConfidence: number;
  diarizationConfidence: number;
  needsReview: boolean;
}

export interface BatchResult {
  batchId: string;
  sessionId: string;
  utterances: DiarizedUtterance[];
  totalDurationMs: number;
  processedAt: Date;
}

export interface DeepgramDiarizedUtterance {
  start: number;
  end: number;
  confidence: number;
  channel: number;
  transcript: string;
  speaker: number;
  id: string;
  words: DeepgramWordWithSpeaker[];
}

export interface DeepgramWordWithSpeaker {
  word: string;
  start: number;
  end: number;
  confidence: number;
  speaker: number;
  speaker_confidence: number;
  punctuated_word: string;
}

<!-- From Plan 01: database.ts new methods -->
async saveDiarizedUtterances(sessionId, batchId, utterances[]): Promise<boolean>
async updateSpeakerMapping(sessionId, mapping): Promise<boolean>
async regenerateRawTextWithSpeakers(sessionId, consultationId, mapping): Promise<boolean>

<!-- Existing presencialSessionManager key signatures -->
class PresencialSessionManager {
  private sessions = new Map<string, PresencialSession>();
  private deepgramClient: ReturnType<typeof createDeepgramClient> | null;
  private transcribeWithDeepgram(audioBuffer, speaker, language, consultationId): Promise<{text, confidence, duration}>
  async createSession(data): Promise<PresencialSession>
  async processAudioChunkAndReturn(sessionId, speaker, audioBuffer, sequence): Promise<Transcription | null>
  async endSession(sessionId): Promise<void>
  private async saveTranscriptionIncrementally(session, transcription): Promise<void>
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add batch accumulator and diarized processing to presencialSessionManager</name>
  <files>apps/backend/realtime-service/src/services/presencialSessionManager.ts</files>
  <read_first>
    - apps/backend/realtime-service/src/services/presencialSessionManager.ts (full file — understand all methods, session lifecycle, existing patterns)
    - apps/backend/realtime-service/src/types/diarization.ts (created in Plan 01)
    - apps/backend/realtime-service/src/config/database.ts (for saveDiarizedUtterances signature)
    - apps/backend/realtime-service/src/websocket/presencial.ts (understand how processAudioChunkAndReturn is called, io reference)
  </read_first>
  <action>
    Refactor `presencialSessionManager.ts` to add batch accumulation as a PARALLEL path alongside existing per-chunk processing. Do NOT remove existing per-chunk processing — it provides immediate unattributed feedback per D-08.

    **Step 1: Add imports at top of file:**
    ```typescript
    import { AudioAccumulator, DiarizedUtterance, BatchResult, DeepgramDiarizedUtterance, DeepgramWordWithSpeaker } from '../types/diarization';
    import crypto from 'crypto';
    ```

    **Step 2: Add accumulator Map as class property (after `private sessions`):**
    ```typescript
    // Audio accumulators for 60s batch diarization (per D-01)
    private accumulators = new Map<string, AudioAccumulator>();

    // Socket.IO server reference for emitting events
    private io: any = null;

    // Confidence threshold for speaker attribution (per D-16, D-18)
    private diarizationConfidenceThreshold: number;
    ```

    **Step 3: In constructor, after existing initialization, add:**
    ```typescript
    this.diarizationConfidenceThreshold = parseFloat(process.env.DIARIZATION_CONFIDENCE_THRESHOLD || '0.7');
    console.log(`[PRESENCIAL] Diarization confidence threshold: ${this.diarizationConfidenceThreshold}`);
    ```

    **Step 4: Add `setIO` method to receive Socket.IO server reference:**
    ```typescript
    setIO(io: any): void {
      this.io = io;
    }
    ```

    **Step 5: Add accumulator lifecycle methods:**

    `startAccumulator(sessionId: string): void` — Per D-01, D-03:
    - Create new AudioAccumulator: `{ chunks: [], timer: null, batchNumber: 0, startTime: new Date(), totalDurationMs: 0 }`
    - Store in `this.accumulators.set(sessionId, acc)`
    - Call `this.scheduleFlush(sessionId)`
    - Log: `[DIARIZATION] Accumulator started for session ${sessionId}`

    `scheduleFlush(sessionId: string): void` — Per D-01, uses setTimeout chain (not setInterval) to prevent overlapping batch processing:
    - Get accumulator from map, return if not found
    - Set `acc.timer = setTimeout(async () => { await this.flushBatch(sessionId); if (this.accumulators.has(sessionId)) { this.scheduleFlush(sessionId); } }, 60_000);`

    `addChunkToAccumulator(sessionId: string, audioBuffer: Buffer): void` — Called from processAudioChunkAndReturn:
    - Get accumulator, return if not found
    - Push audioBuffer to acc.chunks
    - Increment acc.totalDurationMs by 5000 (each chunk is 5s per D-01)
    - Log: `[DIARIZATION] Chunk added to accumulator for ${sessionId} (${acc.chunks.length} chunks, ${acc.totalDurationMs/1000}s)`

    `flushBatch(sessionId: string): Promise<void>` — Per D-01, D-02, D-05:
    - Get accumulator, return if not found or acc.chunks.length === 0
    - Generate batchId: `batch-${sessionId.substring(0,8)}-${acc.batchNumber++}` (keep it short)
    - Copy chunks to local array: `const chunksToProcess = [...acc.chunks];`
    - Clear accumulator immediately: `acc.chunks = []; acc.totalDurationMs = 0;`
    - Concatenate all chunks: `const concatenated = Buffer.concat(chunksToProcess);`
    - Log: `[DIARIZATION] Flushing batch ${batchId}: ${chunksToProcess.length} chunks, ${concatenated.length} bytes`
    - Get session from this.sessions.get(sessionId) — if not found, log error and return
    - Call Deepgram pre-recorded API with utterances:true + diarize:true (see transcribeWithDiarization below)
    - Call processDiarizedUtterances with the result
    - Wrap in try/catch — on error, log `[DIARIZATION] Batch ${batchId} failed: ${err}` but do NOT re-throw (don't crash the timer chain)

    `stopAccumulator(sessionId: string): void`:
    - Get accumulator, return if not found
    - If acc.timer, call `clearTimeout(acc.timer)` — per Pitfall 5 from RESEARCH.md
    - Delete from map: `this.accumulators.delete(sessionId)`
    - Log: `[DIARIZATION] Accumulator stopped for session ${sessionId}`

    **Step 6: Add Deepgram batch transcription method:**

    `private async transcribeWithDiarization(audioBuffer: Buffer, consultationId: string): Promise<DeepgramDiarizedUtterance[]>` — Per D-05, D-06:
    - If !this.deepgramClient, throw error
    - Call `this.deepgramClient.listen.prerecorded.transcribeFile(audioBuffer, { model: 'nova-2', language: 'pt-BR', smart_format: true, punctuate: true, numerals: true, diarize: true, utterances: true, keywords: ['paciente:2', 'doutor:2', 'doutora:2', 'pressao arterial:3', 'frequencia cardiaca:3', 'hemograma:3', 'diagnostico:2', 'medicamento:2'] })`
    - If error, throw
    - Extract utterances: `const utterances = result?.results?.utterances || [];`
    - Log cost tracking (same pattern as existing transcribeWithDeepgram — call aiPricingService.logWhisperUsage)
    - Return utterances cast as DeepgramDiarizedUtterance[]

    **Step 7: Add diarized utterance processing method:**

    `private async processDiarizedUtterances(sessionId: string, batchId: string, utterances: DeepgramDiarizedUtterance[]): Promise<void>` — Per D-10, D-11, D-16:
    - Get session from this.sessions, return if not found
    - Map each utterance to DiarizedUtterance:
      - `speakerId`: `speaker_${utt.speaker}` (0 -> 'speaker_0', 1 -> 'speaker_1') per D-11
      - `transcript`: utt.transcript
      - `startMs`: Math.round(utt.start * 1000)
      - `endMs`: Math.round(utt.end * 1000)
      - `transcriptionConfidence`: utt.confidence
      - `diarizationConfidence`: Average of word-level speaker_confidence — `utt.words.reduce((sum, w) => sum + (w.speaker_confidence || 0), 0) / (utt.words.length || 1)` — per Pitfall 4 from RESEARCH.md (speaker_confidence is on WORDS, not utterances)
      - `needsReview`: diarizationConfidence < this.diarizationConfidenceThreshold per D-16
    - Filter out utterances with empty transcript or that fail `isValidTranscriptionText()` (reuse existing anti-hallucination filter)
    - Call `db.saveDiarizedUtterances(session.callSessionId, batchId, mappedUtterances.map(u => ({ speaker: u.needsReview ? 'unknown' : 'unknown', speakerId: u.speakerId, text: u.transcript, startMs: u.startMs, endMs: u.endMs, confidence: u.transcriptionConfidence, diarizationConfidence: u.diarizationConfidence, needsReview: u.needsReview, doctorName: session.doctorName })))` — Note: speaker is always 'unknown' until doctor maps via DIAR-06
    - Also update transcriptions.raw_text per D-15: `const lines = mappedUtterances.map(u => { const ts = formatMs(u.startMs); return '[' + u.speakerId.toUpperCase() + '] (' + ts + '): ' + u.transcript; }); db.appendConsultationTranscription(session.consultationId, lines.join('\n'), 'BATCH', new Date().toISOString().substring(11,19));`
    - Emit `presencialDiarizedBatch` event per D-09: `if (this.io) { this.io.to(sessionId).emit('presencialDiarizedBatch', { sessionId, batchId, utterances: mappedUtterances.map(u => ({ speakerId: u.speakerId, text: u.transcript, startMs: u.startMs, endMs: u.endMs, diarizationConfidence: u.diarizationConfidence, needsReview: u.needsReview })) }); }`
    - Log: `[DIARIZATION] Batch ${batchId} processed: ${mappedUtterances.length} utterances`

    Helper: `private formatMs(ms: number): string` — convert ms to HH:mm:ss format:
    ```typescript
    const totalSecs = Math.floor(ms / 1000);
    const hh = String(Math.floor(totalSecs / 3600)).padStart(2, '0');
    const mm = String(Math.floor((totalSecs % 3600) / 60)).padStart(2, '0');
    const ss = String(totalSecs % 60).padStart(2, '0');
    return `${hh}:${mm}:${ss}`;
    ```

    **Step 8: Modify `processAudioChunkAndReturn` to also feed the accumulator:**

    After the existing line `session.totalChunks++;` (around line 282), add:
    ```typescript
    // Feed accumulator for batch diarization (parallel path per D-08)
    this.addChunkToAccumulator(sessionId, audioBuffer);
    ```

    The rest of processAudioChunkAndReturn stays UNCHANGED — it continues to provide immediate unattributed transcription per D-08. The `speaker` parameter from the caller will continue to be used for the immediate path (it comes from mic assignment in dual-mic mode, or could be 'doctor' as default).

    **Step 9: Modify `createSession` to start accumulator:**

    After `this.sessions.set(data.sessionId, session);` add:
    ```typescript
    // Start batch accumulator for diarization (per D-01)
    this.startAccumulator(data.sessionId);
    ```

    **Step 10: Modify `endSession` to flush and stop accumulator:**

    At the beginning of endSession, BEFORE `session.status = 'ended';`, add:
    ```typescript
    // Flush remaining audio in accumulator and stop timer (per D-03, D-04)
    const acc = this.accumulators.get(sessionId);
    if (acc && acc.chunks.length > 0) {
      console.log(`[DIARIZATION] Flushing remaining ${acc.chunks.length} chunks on session end`);
      await this.flushBatch(sessionId);
    }
    this.stopAccumulator(sessionId);
    ```

    **CRITICAL ANTI-PATTERN AVOIDANCE:**
    - Do NOT modify `deepgramService.ts` — that's for streaming/dual-mic (WebRTC). Per RESEARCH.md anti-patterns.
    - Do NOT remove existing `saveTranscriptionIncrementally` calls — they handle the immediate path per D-08.
    - Do NOT assume speaker IDs are consistent across batches — per Pitfall 6 from RESEARCH.md.
  </action>
  <verify>
    <automated>grep -n "accumulators\|flushBatch\|transcribeWithDiarization\|processDiarizedUtterances\|addChunkToAccumulator\|startAccumulator\|stopAccumulator\|presencialDiarizedBatch\|DIARIZATION_CONFIDENCE_THRESHOLD" apps/backend/realtime-service/src/services/presencialSessionManager.ts | head -30</automated>
  </verify>
  <acceptance_criteria>
    - presencialSessionManager.ts contains "private accumulators = new Map<string, AudioAccumulator>()"
    - presencialSessionManager.ts contains "import { AudioAccumulator" from '../types/diarization'
    - presencialSessionManager.ts contains "private async flushBatch(sessionId: string)"
    - presencialSessionManager.ts contains "private async transcribeWithDiarization("
    - presencialSessionManager.ts contains "utterances: true" in the Deepgram API call options
    - presencialSessionManager.ts contains "private async processDiarizedUtterances("
    - presencialSessionManager.ts contains "w.speaker_confidence" (word-level, NOT utterance-level)
    - presencialSessionManager.ts contains "DIARIZATION_CONFIDENCE_THRESHOLD"
    - presencialSessionManager.ts contains "this.addChunkToAccumulator(sessionId, audioBuffer)" inside processAudioChunkAndReturn
    - presencialSessionManager.ts contains "this.startAccumulator(" inside createSession
    - presencialSessionManager.ts contains "this.stopAccumulator(" inside endSession
    - presencialSessionManager.ts contains "presencialDiarizedBatch" event emission
    - presencialSessionManager.ts contains "clearTimeout(acc.timer)" in stopAccumulator
    - presencialSessionManager.ts still contains "saveTranscriptionIncrementally" (immediate path preserved)
    - presencialSessionManager.ts contains "setIO(" method
  </acceptance_criteria>
  <done>presencialSessionManager.ts has dual-path processing: immediate unattributed transcription (existing) + 60s batch accumulation with diarization (new). Accumulator lifecycle is managed (start on createSession, flush+stop on endSession). Confidence threshold is configurable via env var. Socket.IO events are emitted for both paths.</done>
</task>

</tasks>

<verification>
- Accumulator starts when session is created, stops when session ends
- 60s timer uses setTimeout chain (not setInterval) to prevent overlap
- flushBatch concatenates chunks with Buffer.concat and calls Deepgram with utterances:true + diarize:true
- Speaker confidence is extracted from WORD level (not utterance level)
- Threshold of 0.7 is applied; below threshold = needs_review=true, speaker='unknown'
- Existing immediate transcription path is UNTOUCHED
- endSession flushes remaining buffer before stopping
- Timer is cleared in stopAccumulator to prevent Pitfall 5
</verification>

<success_criteria>
- Audio chunks are accumulated in memory and flushed every 60s
- Deepgram API receives utterances:true + diarize:true for batch audio
- Each utterance is stored as individual row in transcriptions_med with speaker_id, diarization_confidence, batch_id, needs_review
- presencialDiarizedBatch event is emitted to Socket.IO room with diarized utterances
- Immediate presencialTranscription events continue to work (no regression)
- Session end flushes remaining audio and cleans up timer
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-diarization/02-02-SUMMARY.md`
</output>
